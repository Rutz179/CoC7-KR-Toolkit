# CoC7-KO 변경 이력

## 1.1.1 — 서버 설치 보완

- **`/data`에 설치된 Foundry도 찾습니다.** 설치 명령이 `~`, `/opt`, `/srv`만 찾아서
  `/data/foundryvtt/Data`처럼 흔한 서버 경로에 있는 모듈을 놓쳤습니다. `/data`, `/home`, `/mnt`,
  `/var/lib`도 찾습니다.
- **`.env` 파일이 항상 우선합니다.** pm2는 처음 켤 때의 환경을 저장해 두었다가 재시작할 때마다
  돌려줍니다. 브리지가 그 값을 파일보다 우선했기 때문에, 한 번 빈 값으로 켜진 뒤에는 `.env`를
  고쳐도 재시작만으로 반영되지 않았습니다. 이제 `.env`를 고치고 `pm2 restart`만 하면 됩니다.

## 1.1.0 — 음성 강조 전체 공유 · 노트 튕김 · 초상화 간격 · 설정 도우미 개편

### 음성 강조가 키퍼 화면에서만 보이던 문제
`module.json`에 **`"socket": true`가 없었습니다.** Foundry 서버는 이 선언이 있는 모듈의 소켓
메시지만 다른 참가자에게 전달합니다. 선언이 없으면 메시지를 조용히 버리므로, 키퍼는 자기 화면에
직접 적용해서 보였지만 플레이어에게는 아무것도 도착하지 않았습니다.

- 매니페스트에 소켓을 선언했습니다.
- 중계하는 키퍼가 **어느 초상화를 켤지까지 계산해서** 보냅니다. 플레이어는 권한이 없어 볼 수
  없는 캐릭터를 직접 찾을 필요가 없습니다.

### 여러 명이 동시에 노트를 쓰면 튕기던 문제
누군가 노트를 올릴 때마다 모든 사람의 노트 창 **전체를** 다시 그렸습니다. 입력 중이던 칸까지
새로 만들어지면서 쓰던 글이 임시 저장본으로 되돌아가고 커서가 빠졌습니다. 이제 새 메시지가 오면
**목록 부분만** 바꾸고 입력 칸은 그대로 둡니다. 읽던 위치도 유지하며, 맨 아래를 보고 있었다면
새 글로 따라 내려갑니다. 귓속말 창도 같은 방식으로 고쳤습니다.

### 인벤토리를 열면 초상화가 작아지던 문제
초상화 자동 맞춤이 카드 **전체 높이**를 쟀는데, 인벤토리를 열면 능력치와 아이템 칸이 붙어
카드가 길어집니다. 이제 초상화와 이름표만 잽니다.

### PC 3·4번 간격
3·4번이 화면 높이의 **정확한 가운데**에 놓였는데, 아래쪽 여백(핫바)이 위쪽보다 훨씬 커서 1·2번과
5·6번 사이의 실제 가운데는 그보다 위에 있었습니다. 그래서 3·4번이 아래로 쏠려 5·6번과 붙었습니다.
이제 **위아래 줄 사이 공간의 가운데**에 두어 양쪽 간격이 같습니다.

### 디스코드 설정 도우미 개편
- **설정 파일 `.env`를 모듈 폴더 안에** 만들고, 브리지가 그 파일을 스스로 읽습니다. 실행할 때
  추가 옵션이 필요 없습니다.
- 서버 설치 명령을 **짧은 한 줄짜리 다섯 개**로 나누고 각각 복사 버튼을 달았습니다. 여러 줄을 한
  번에 붙여넣다 깨지던 문제가 없어집니다. 모듈 폴더는 입력한 경로가 틀려도 스스로 찾습니다.
- **처음부터 다시** 버튼: 입력값·브리지 주소·연결 키를 지우고 연동을 끕니다. 서버에 설치한 브리지를
  지우는 한 줄 명령도 첫 화면에 있습니다.
- **Node.js와 설치 파일은 사용자가 그 컴퓨터에서 직접** 설치하도록 안내합니다. 음성 암호화
  라이브러리는 운영체제·CPU마다 다른 파일이 들어 있어, 다른 컴퓨터의 `node_modules`를 복사해 오면
  동작하지 않습니다.
- 연결 확인 단계에서 `.env`가 **인터넷에서 읽히지 않는지 실제로 요청해 확인**하고, 읽히면 즉시
  경고합니다.
- 입력 칸의 예시 값을 일반적인 숫자로 바꿨습니다.

### 배포 정리
- Oracle 서버 관리자 프로그램과 관련 안내를 배포본에서 제거했습니다.
- 배포본에 `node_modules`와 `.git`이 들어가지 않도록 `.gitignore`를 추가했습니다.

## 1.0.6 — 설정 도우미 단계 건너뜀 수정

### 도우미가 3단계를 건너뛰고, 이전을 누르면 첫 화면으로 가던 문제
창이 다시 그려질 때마다 클릭 처리기를 **하나씩 더** 붙이고 있었습니다. 창의 내용 영역은 다시
그려도 같은 요소로 남기 때문에 처리기가 계속 쌓였고, 한 번의 클릭이 여러 번 실행되었습니다.
두 번째 화면의 "다음"이 두 칸을 넘어가고, 네 번째 화면의 "이전"이 첫 화면까지 돌아간 것이
이 때문입니다. 처리기를 한 번만 붙이도록 고쳤습니다.

**연결 관리 창**에도 같은 문제가 있었습니다(사용자 추가를 누르면 줄이 여러 개 생김). 함께
고쳤습니다.

### v14 배경색 경고
스테이지 타일을 만든 뒤 씬 배경색을 맞추던 동작이 v14에서 `Scene#backgroundColor is
deprecated` 경고를 냈습니다. v14에서는 배경색이 Level 문서로 옮겨졌고 이 쓰기는 무시되며,
스테이지 배경 타일이 캔버스 전체를 덮으므로 필요도 없습니다. Level이 없는 이전 코어에서만
실행합니다.

## 1.0.5 — 서버 설정 도우미 보완

### 브리지 주소에 `내도메인`이 그대로 들어가던 문제
서버 방식에서 도메인 칸이 비어 있으면 자리표시 글자가 실제 주소로 저장되어, 접속할 수 없는
`wss://내도메인/...` 주소가 만들어졌습니다.

- 도메인을 **지금 접속한 주소에서 자동으로 채웁니다.** Foundry와 브리지는 같은 Caddy 사이트
  뒤에 있으므로 대부분 그대로 두면 됩니다. localhost와 IP 주소는 제외합니다.
- 도메인이 비어 있으면 주소를 적용하지 않고 안내합니다. 자리표시 글자가 주소에 들어가는 경우는
  더 이상 없습니다.

### 설치 명령이 모듈 폴더를 스스로 찾음
서버의 Foundry 데이터 경로가 기본값과 다르면 설치 명령이 실패했습니다. 이제 입력한 경로 →
흔히 쓰는 위치(`~/foundrydata`, `~/foundryuserdata`, `~/.local/share/FoundryVTT`,
`/opt/foundrydata`) → 홈 폴더 검색 순서로 찾습니다. 모듈이 없으면 이유를 알려주고 멈추며,
SSH 접속은 끊기지 않습니다.

### "설치"라는 점을 분명히
3단계의 SSH 명령이 **설치**이며 한 번은 꼭 실행해야 pm2에 브리지가 생긴다는 점을 강조했습니다.

## 1.0.4 — 디스코드 음성 연동 켜기/끄기

브리지 주소를 한 번 설정해 두면 브리지가 꺼져 있어도 매번 접속을 시도하고, 30초 간격으로
끝없이 재시도하며 접속 실패 로그를 남기던 문제를 고쳤습니다.

### 대분류 스위치
모듈 설정 맨 앞에 **`디스코드 음성 연동 사용`**(월드 설정, 기본 꺼짐)을 추가했습니다.

- 끄면 브리지에 **전혀 접속하지 않습니다.** 재시도도, 로그도 없습니다.
- 설정 도우미의 **이 주소로 연결**을 누르면 자동으로 켜집니다.
- 이미 연동을 쓰고 있었다면 업데이트 후 **한 번 켜 주셔야** 합니다.

### 켜져 있어도 조용하게
- 브리지를 **5번 연속** 찾지 못하면 재시도를 멈춥니다. 브리지를 켠 뒤 연결 관리 창이나
  도우미의 **다시 연결**을 누르면 처음부터 다시 시도합니다.
- 접속 실패는 한 번만 기록합니다. 브라우저가 실패한 연결마다 자체 경고를 남기므로 모듈이
  같은 내용을 반복할 이유가 없었습니다. 기록에 남는 주소에서 연결 키는 가립니다.
- 연결 상태 표시에 `사용 안 함`, `재시도 멈춤` 상태를 추가했습니다.

## 1.0.3 — 파란 상자 / 스테이지 이미지 사라짐 수정

1.0.1에서 씬 전체에 **파란 상자**가 깔리고 스테이지 이미지가 나타나지 않던 문제를 고쳤습니다.

### 원인
- 1.0.0에서 넣은 "투명" 자리표시 이미지가 실제로는 투명하지 않았습니다. 픽셀 값이
  `rgba(0, 0, 255, 127)`, 즉 **반투명 파란색**이었습니다. 1.0.0에서는 v14가 배경 쓰기를 조용히
  버려서 드러나지 않았고, 1.0.1에서 Level 경로로 쓰기가 실제로 적용되면서 나타났습니다.
- 1.0.1은 스테이지 타일을 만들기 **직전에** Level 문서를 수정했습니다. v14에서 이 수정은
  캔버스 재구성을 일으킬 수 있어, 그 과정에서 타일 생성이 끊긴 것으로 보입니다.

### 조치
- 모듈은 더 이상 **보이는 모양을 위해 씬 배경 문서를 수정하지 않습니다.** 씬 배경 처리는
  1.0.0 이전 동작으로 되돌렸습니다.
- 씬 테두리는 1.0.1의 구조 기반 탐색으로 **화면에 그릴 때만** 감춥니다. 월드 데이터는 바뀌지
  않습니다.
- 이미 파란 자리표시 이미지가 들어간 장면은 키퍼가 장면을 열 때 **그 이미지만 골라 지웁니다.**
  키퍼가 직접 고른 배경은 건드리지 않습니다.
- 자리표시 이미지 파일은 픽셀까지 `rgba(0, 0, 0, 0)`인 것을 확인한 파일로 교체했습니다.
  정리 전 잠깐 보이더라도 아무것도 그려지지 않습니다.

## 1.0.2 — 로컬 .bat을 모듈 폴더 방식으로 되돌림

1.0.0에서 `.bat`이 Foundry 서버에서 브리지 파일을 내려받도록 바꾼 것은 **불필요한 변경**이었습니다.
"bridge.js를 찾지 못했습니다" 오류는 `.bat`을 모듈 폴더 밖에 두고 실행해서 생긴 것이었고,
로컬 방식은 Foundry가 같은 PC에 있어 모듈 폴더가 있는 것이 전제입니다.

- `.bat`은 다시 **모듈 폴더(`tools/discord-voice-bridge`) 안에서** 실행됩니다. 다른 곳에
  두었다면 Foundry 기본 데이터 폴더의 모듈 경로를 찾아봅니다.
- 서버에서 파일을 내려받는 동작과 별도 설치 폴더(`%LOCALAPPDATA%\CoC7-KO`)는 없앴습니다.
- 도우미의 파일 만들기 단계가 **저장 위치로 모듈 폴더를 안내**합니다. 경로를 복사해 저장 창의
  주소 칸에 붙여넣으면 바로 그 폴더로 이동합니다.

1.0.0/1.0.1의 `.bat`을 실행했다면 `%LOCALAPPDATA%\CoC7-KO\voice-bridge` 폴더가 생겼을 수
있습니다. 지워도 됩니다.

## 1.0.1 — 씬 테두리 근본 수정 (v14)

앞선 두 번의 시도가 실패한 원인을 모두 확인했습니다.

### 1. 테두리 객체는 이름으로 찾을 수 없다
테두리는 `InterfaceCanvasGroup`의 `#outline`, 즉 자바스크립트 **비공개 필드**입니다. 비공개
필드는 바깥 코드에서 이름으로 절대 접근할 수 없으므로 `canvas.interface.outline`을 찾던
0.9.8의 방식은 처음부터 불가능했습니다. v14 공개 API 문서에서도 이 항목은 빠져 있습니다.

그래도 화면에 그려지려면 인터페이스 그룹의 **자식**으로 붙어 있어야 합니다. 그리고 그 자식
중 격자 같은 레이어(CanvasLayer)도, 그림·스크롤 텍스트용 컨테이너도 아닌 **순수한
PIXI.Graphics는 테두리뿐**입니다. 이제 이름이 아니라 이 구조로 찾아서 감춥니다.

### 2. v14는 `Scene#background` 쓰기를 조용히 버린다
v14는 씬 배경을 **Level 문서**로 옮겼습니다. `Scene#background`에 값을 써도 오류 없이
무시됩니다. 1.0.0의 투명 배경 방식이 효과가 없었던 이유이고, 그보다 앞서 "스테이지 이미지와
겹치지 않도록 씬 기본 배경을 비우는" 동작도 v14에서는 계속 조용히 실패하고 있었습니다.
배경을 읽고 쓰는 모든 경로를 Level 문서를 통하도록 바꿨습니다. Level이 없는 이전 코어에서는
기존 방식을 그대로 씁니다.

### 진단 명령
그래도 선이 남아 있다면 아래를 실행해 결과를 확인할 수 있습니다. 인터페이스 층의 모든 요소와
위치·크기, 테두리로 판정된 항목, 현재 씬 배경 경로를 보여줍니다.

```js
game.coc7koStage.inspectCanvasChrome()
```

## 1.0.0 — 첫 배포

### 씬 테두리가 여전히 보이던 문제
0.9.8에서는 v14의 테두리 객체 이름을 추측해 숨기려 했는데, 그 이름이 맞지 않았습니다.
이번에는 Foundry 내부를 건드리지 않고 **Foundry 자신의 규칙**을 이용합니다. 테두리는 씬에
기본 배경 이미지가 없을 때만 그려지므로, 씬 기본 배경을 비우는 대신 **완전히 투명한 1×1
이미지**(`assets/transparent.png`)로 채웁니다. Foundry는 배경이 있는 씬으로 인식해 테두리를
그리지 않고, 화면에는 아무것도 보이지 않으며 스테이지 이미지와 겹치지도 않습니다. 장면 설정
창은 이 자리표시 이미지를 원래 배경으로 착각하지 않습니다.

### 로컬 브리지: Foundry가 서버에 있어도 동작
`.bat`이 PC의 Foundry 모듈 폴더에서 브리지를 찾았기 때문에, Foundry를 서버에서 돌리는 경우
"bridge.js를 찾지 못했습니다" 오류가 났습니다.

- 이제 브리지 파일을 **Foundry 서버에서 직접 내려받습니다.** Foundry는 모듈 파일을 웹으로
  제공하므로, Foundry가 PC에 있든 서버에 있든 똑같이 동작합니다.
- 브리지는 `%LOCALAPPDATA%\CoC7-KO\voice-bridge`라는 별도 폴더에 둡니다. 모듈을 업데이트해도
  설치된 라이브러리가 지워지지 않습니다.
- 실행할 때마다 서버에서 최신 `bridge.js`를 받아오므로, 모듈을 업데이트하면 브리지도 따라
  업데이트됩니다. 서버에 닿지 않으면 가지고 있는 사본으로 실행합니다.

### 자동 실행 제거
Windows 시작 시 자동 실행을 기본으로 켜던 동작을 없앴습니다. 원하는 사용자를 위해 도우미에
**직접 시작 프로그램 폴더에 넣는 방법**(`Win+R` → `shell:startup`)을 접힌 안내로 넣었습니다.

0.9.9의 `.bat`을 한 번이라도 실행했다면 시작 프로그램 폴더에 사본이 들어가 있습니다.
`Win+R` → `shell:startup`에서 `coc7-voice-bridge.bat`을 지워 주세요.

### 브리지 라이브러리 버전
`@discordjs/voice`를 `^0.18.0`으로 고정하고 있었는데, 0.x 버전에서 `^`는 0.18.x만 허용합니다.
디스코드는 음성 암호화 방식(DAVE)을 계속 바꾸고 있어 옛 버전은 연결되지 않습니다. 새로 설치하는
사용자가 항상 최신 음성 라이브러리를 받도록 바꿨습니다.

## 0.9.10 — .bat 저장 위치 선택

`.bat`을 받으면 어디에 저장됐는지 알 수 없던 문제를 고쳤습니다. 브라우저의 일반 다운로드
방식은 파일을 다운로드 폴더에 조용히 넣거나, Foundry 데스크톱 앱에서는 저장 창 없이
사라지게 만들 수 있었습니다.

- 이제 버튼을 누르면 **저장할 위치를 고르는 창**(다른 이름으로 저장)이 뜹니다. 기본 위치는
  바탕화면입니다.
- 저장이 끝나면 도우미에 **저장한 파일 이름**과 다음 할 일(더블클릭)을 표시합니다.
- 저장 창을 쓸 수 없는 환경(http://IP 주소로 접속한 경우 등 — 브라우저가 보안상 막습니다)
  에서는 다운로드 폴더에 저장하고, **그 위치를 찾는 방법**을 안내합니다.

## 0.9.9 — 설정 도우미: 브리지 켜는 법 안내 · 자동 실행

도우미 마지막 단계가 "브리지가 켜져 있는지 확인하세요"라고만 해서, 무엇을 해야 하는지
알 수 없었습니다.

- **로컬**: 받은 `.bat`을 더블클릭하라고 구체적으로 안내하고, 그 자리에서 다시 받을 수
  있는 버튼을 둡니다. 웹페이지는 보안상 PC의 프로그램을 직접 실행할 수 없어서 이 한 번의
  더블클릭은 사람이 해야 한다는 점도 설명합니다.
- **서버**: pm2가 이미 계속 실행하고 있으므로 켤 필요가 없다고 안내하고, 연결이 안 될 때
  확인할 로그와 명령을 알려줍니다.
- 마지막 단계가 **연결 상태를 스스로 갱신**합니다. 브리지가 켜지는 순간 "연결됨"으로 바뀝니다.

### Windows 시작 시 자동 실행 (로컬)
파일 만들기 단계에 `Windows를 켤 때마다 자동으로 실행` 선택지를 추가했습니다(기본 켜짐).
켜면 `.bat`이 처음 실행될 때 자기 자신을 시작 프로그램 폴더에 복사하므로, **처음 한 번만
더블클릭**하면 이후로는 PC를 켤 때 알아서 실행됩니다. 그만두려면 `Win+R` → `shell:startup`
에서 파일을 지우면 됩니다.

브리지 창은 항상 **최소화된 채로** 뜹니다. 작업 표시줄에만 보이고 화면을 가리지 않습니다.

## 0.9.8 — 초상화 자동 맞춤 · 씬 테두리 숨김 · 서버 블러 수정

### 초상화가 해상도에 따라 겹치던 문제
카드 폭은 화면 **폭**(`12.5vw`)만 보고 정해지는데, 겹침은 **세로**에서 생겼습니다. 좌우
각 열에 카드가 위·가운데·아래 세 장 쌓이는데 화면 높이는 고려하지 않아서, 가로로 넓고
세로로 낮은 창일수록 겹쳤습니다.

- 설정의 **초상화 크기**는 이제 **최대 크기**로 쓰입니다.
- 새 설정 `초상화 크기 자동 맞춤`(기본 켜짐): 세로 공간이 모자라면 겹치지 않을 만큼만
  모든 카드를 함께 줄이고, 공간이 넉넉해지면 원래 크기로 돌아갑니다.
- 실제로 카드가 있는 자리만 따집니다. 가운데 자리가 비어 있으면 위·아래 두 장만 기준으로
  계산하므로 불필요하게 작아지지 않습니다.
- 창 크기 변경, 사이드바 접기, 카드 추가·제거 때마다 다시 계산합니다.
- 배치 편집으로 직접 옮긴 카드는 지정한 크기를 그대로 유지합니다.

### 씬 테두리(검은 실선 박스)가 이미지 위에 보이던 문제
Foundry는 씬에 **기본 배경 이미지가 없으면** 씬 영역에 테두리를 그립니다
(`Interface#drawOutline`). 이 모듈은 이미지가 겹쳐 보이지 않도록 씬 기본 배경을 일부러
비우고 타일을 쓰므로, 스테이지 씬은 늘 "배경 없는 씬"으로 취급되어 테두리가 켜져 있었습니다.

이 선은 타일보다 **위에 있는 인터페이스 층**에 그려지기 때문에 정렬 순서로 뒤로 보낼 수
없습니다. 그래서 스테이지 이미지가 있는 동안에는 테두리를 감춥니다. 이미지를 지우면 원래대로
돌아옵니다. 보이기 여부만 바꿀 뿐 아무것도 지우지 않습니다.

같은 층에 있는 **격자선**도 새 설정 `스테이지 이미지 위 격자 숨기기`(기본 켜짐)로 감출 수
있습니다. 토큰의 칸 맞춤은 그대로 동작합니다.

### 블러 사본이 서버에서만 실패하던 문제
원인을 하나로 확정하지는 못했지만, 로컬과 서버의 차이에서 가능성이 큰 세 곳을 모두 고쳤습니다.

- **파일 이름**: 원본 이름을 따서 만들었기 때문에 한글·공백이 그대로 들어갔습니다. Windows는
  너그럽지만 Linux 서버에서는 저장된 경로와 되돌려받은 URL의 인코딩이 어긋나 새 타일이 파일을
  못 찾을 수 있습니다. 이제 원본 경로의 짧은 해시로 **영문 이름**을 만듭니다.
- **불러오기**: `<img>` + 캔버스 방식은 리버스 프록시 뒤에서 CORS 제약에 걸릴 수 있습니다.
  같은 출처에서 파일을 받아 `createImageBitmap`으로 여는 방식으로 바꿔 CORS가 필요 없습니다.
- **진단**: 실패하면 `[불러오기 실패]` / `[블러 처리 실패]` / `[업로드 실패]` 중 어느 단계인지와
  원인을 알림으로 보여줍니다. 업로드 권한이 없는 계정이면 미리 알려줍니다.

### 장면 퀵바
한 장면 안에서 배경만 바꿔 쓰는 방식이 자리 잡아, **장면 북마크 섹션을 제거**했습니다.
장소 프리셋, 도구, 창 버튼은 그대로입니다.

## 0.9.7 — 설정 도우미 · 키퍼 중계 방식 · 화자 해제

### 디스코드 음성 연동 설정 도우미
모듈 설정 → **디스코드 음성 연동 설정 도우미**. 단계별로 안내하며 필요한 파일과 명령을
값이 채워진 상태로 만들어 줍니다.

1. **방식 선택** — 내 PC(로컬) / Oracle 등 서버(pm2 + Caddy)
2. **디스코드 봇** — 개발자 포털 안내, 토큰·서버 ID·채널 ID 입력
3. **파일 만들기**
   - 로컬: `start-voice-bridge.bat` 다운로드. 더블클릭하면 Node.js 확인 → 첫 설치 →
     실행까지 알아서 합니다. Foundry 기본 데이터 폴더에서 브리지를 스스로 찾으므로
     파일을 바탕화면에 두어도 됩니다.
   - 서버: SSH에 **한 번 붙여넣으면 끝나는 명령 덩어리**와 Caddy 설정 조각. 복사 버튼 제공.
4. **연결 확인** — 주소를 한 번에 적용하고 실시간 연결 상태 표시, 사용자 연결 창으로 이동.

연결 키는 브라우저에서 무작위로 생성합니다. **봇 토큰은 어디에도 저장하지 않습니다.**
월드 설정은 모든 플레이어에게 전송되기 때문입니다.

### 키퍼 한 명이 중계하는 구조로 변경
이전에는 모든 클라이언트가 브리지에 직접 접속했습니다. 이 방식은 로컬 호스팅에서
동작하지 않았습니다. `ws://127.0.0.1:8787`은 호스트 화면에서만 호스트를 가리키고,
플레이어 PC에서는 **플레이어 자기 컴퓨터**를 가리키기 때문입니다.

이제 첫 번째로 접속한 키퍼만 브리지에 연결하고, 받은 신호를 Foundry 자체 소켓으로 모든
플레이어에게 전달합니다.

- 로컬 호스팅: 8787 포트포워딩이 필요 없습니다.
- 서버 호스팅: 플레이어 쪽 연결 문제가 사라집니다.
- 중계 키퍼가 나가면 다음 키퍼가 자동으로 이어받습니다.

### 화자 지정 해제
원인: 초상화를 **한 번 클릭하기만 해도** 화자로 지정되고, 이 지정은 **누군가 채팅을 칠
때까지** 풀리지 않았습니다. 실수로 누르기는 쉽고 풀 방법은 없었습니다.

- 현재 화자 초상화를 **다시 클릭하면 해제**됩니다.
- 우클릭 메뉴: 상황에 따라 `화자로 지정` / `화자 지정 해제`, 그리고 `모든 화자 해제`.
- 장면 퀵바에 **화자 해제** 버튼. 수동 지정·채팅 강조·음성 강조를 한 번에 풉니다.
- 음성 강조가 풀리는 시간을 8초 → 5초로 줄였습니다(브리지 재전송 주기 3초).

### 문서
브리지 README의 서버 배포 절차를 nginx + systemd에서 **pm2 + Caddy**로 바꿨습니다.

## 0.9.6 — 로컬 마무리: 동시 발화 · 연결 관리 창 · 브리지 안정화

### 사용자 제공 수정 반영
`visual-novel-stage.js`는 사용자가 직접 고친 버전을 기준으로 삼았습니다. v14 타일이
중심 기준 좌표(`anchorX/Y: 0.5`)를 쓰는 점을 반영해 배경·중앙 이미지가 제자리에 놓입니다.

### 동시 발화
음성 하이라이트가 채팅의 단일 초점을 빌려 쓰던 구조를 바꿨습니다. 이전에는 두 명이 겹쳐
말하면 하이라이트가 둘 사이를 오갔고, 먼저 멈춘 사람이 아직 말하고 있는 사람의 초상화까지
꺼버렸습니다. 이제 말하는 사람마다 따로 켜지고 따로 꺼집니다.

### 켜진 채로 남는 초상화 방지
- 브리지가 말하는 동안 3초마다 신호를 다시 보냅니다.
- Foundry는 8초 동안 신호가 없으면 초상화를 끕니다.
- 누군가 말하는 도중 음성방을 나가면 브리지가 즉시 "멈춤"을 보냅니다.
- 브리지 연결이 끊기면 켜져 있던 초상화를 모두 끕니다.

주기적 재전송이 필요한 이유: 디스코드는 말하기의 시작과 끝에만 신호를 보냅니다. 안전
타이머만 두면 10초짜리 긴 대사 중간에 초상화가 꺼집니다.

### 디스코드 ↔ Foundry 연결 관리 창
콤마로 한 줄에 몰아 적던 방식을 없앴습니다. 모듈 설정의 **연결 관리** 버튼으로 창을 엽니다.

- 한 줄에 한 명씩, Foundry 사용자는 목록에서 고르고 디스코드 ID를 적습니다.
- **최근 음성 신호** 칸: 음성 채널에서 말하면 들린 디스코드 ID가 표시되고, **+**로 바로
  줄을 추가합니다. ID를 직접 찾아 복사할 필요가 없습니다.
- 상단에 연결 상태(연결됨 / 재연결 대기 / 주소 오류), 각 줄에 말하는 중 표시(초록 점).
- 저장할 때 ID 형식과 중복을 검사합니다.
- 기존 콤마 방식으로 적어둔 값은 첫 실행 때 자동으로 옮겨집니다.

### 브리지 봇
- discord.js의 `ready` → `clientReady` 이름 변경에 대응했습니다. 설치된 버전이 제공하는
  쪽을 골라 쓰므로 경고가 사라지고 다음 메이저 버전에서도 그대로 동작합니다.
- README에 로컬 확인 목록과 Oracle 배포 절차(npm ci, 환경 파일, nginx WSS, systemd)를
  정리했습니다.

## 0.9.5 — 좌측 여백 · 링크 형식 · 스테이지 배경 · 폴더 · 퀵바 · 디스코드

### 좌측 여백이 반영되지 않던 이유
CSS 변수를 아무리 바꿔도, 스테이지가 크기를 맞출 때마다 `--coc7ko-safe-left`를
**인라인으로 덮어쓰고** 있었습니다. 그 값은 `#controls` 요소 폭으로 계산했는데 v14에는
그 요소가 없어서 항상 기본값 64px이 들어갔습니다. 이제 씬 컨트롤 두 열과 플레이어 목록까지
실제로 보이는 좌측 요소를 모두 측정해 가장 넓은 쪽을 비켜갑니다. 여유 간격은 설정
`포트레잇 좌측 여유 간격`으로 조절합니다.

배치 편집 모드로 초상화를 직접 옮긴 적이 있다면 그 위치가 우선합니다. 배치 편집의
**초기화**를 눌러야 새 기본 위치가 적용됩니다.

### 판정 팔레트: 이상적인 링크 형식으로 저장
끌어다 놓은 링크를 시스템의 enricher 문법으로 변환해 저장합니다.

```
@coc7.sanloss[sanMin:1,sanMax:1d12,sanReason:신화생물을 만남,difficulty:2]{신화생물을 만남}
@coc7.check[type:skill,name:관찰력,difficulty:2,modifier:1]{관찰해라}
```

드래그 페이로드의 속성 이름은 enricher 매개변수와 두 곳이 다릅니다
(`subtype`→`type`, `poolModifier`→`modifier`). 표시용 필드는 버립니다. 이미 `@coc7.`로
시작하는 텍스트는 손대지 않고 저장합니다.

### 스테이지 배경 (코코포리아식)
- **배경**은 씬 바깥 여백까지 캔버스 전체를 채웁니다.
- **중앙 이미지**는 플레이어가 보는 씬 영역을 정확히 채웁니다. 크기를 줄이면 씬 비율을
  유지한 채 가운데로 작아집니다. 16:9 강제는 없앴습니다.
- 씬이 로드될 때마다 타일 위치를 다시 맞춥니다. 씬 크기나 여백을 바꾼 뒤 이미지가 엉뚱한
  곳에 남던 문제가 해결됩니다.
- 이전에 저장한 장면은 중앙 이미지 크기가 72%로 저장되어 있을 수 있습니다. 슬라이더를
  100%로 올려주세요.

### 배경 블러
**블러 사본 만들기** 버튼이 생겼습니다. 배경 이미지를 흐리게 처리한 사본을
`worlds/<월드>/coc7ko-blur/`에 저장하고 배경으로 교체합니다. 원본은 그대로 남습니다.

실시간 필터 대신 미리 구운 이미지를 쓰는 이유: 타일은 WebGL로 그려져 CSS 필터가
통하지 않고, PIXI BlurFilter는 v12 이후 타일이 일괄 렌더링되면서 무시되거나 새로
그릴 때마다 사라지며, 캔버스 전체를 매 프레임 흐리는 것은 비용이 큽니다. 한 번 구운
이미지는 모든 클라이언트에서 똑같이 보이고 실행 중 부담이 없습니다.

### 팔레트 폴더
NPC·아이템 팔레트에 **폴더 선택**이 생겼습니다. 별도의 폴더 시스템을 만들지 않고
Foundry의 기존 폴더를 그대로 따릅니다.

- 액터 탭에서 세션별 폴더를 만들고 NPC를 넣으면 NPC 팔레트에서 그 폴더만 볼 수 있습니다.
- 컴펜디움 아이템을 아이템 탭의 폴더로 끌어다 놓으면 월드 사본이 그 폴더에 생기고,
  아이템 팔레트에서 폴더로 고를 수 있습니다. 컴펜디움 내부 폴더도 인식합니다.
- 마지막으로 고른 폴더는 클라이언트별로 기억됩니다. 세션방마다 자기 폴더로 열립니다.

### 장면 퀵바
- 키퍼 화면에 **기본으로 표시**됩니다. 설정 `장면 퀵바 표시`로 끌 수 있습니다.
- 옮기기 전까지 **화면 중앙 상단 또는 하단**(설정 `장면 퀵바 기본 위치`)에 놓입니다.
- 북마크한 장면이 **씬 폴더별로 묶여** 표시됩니다.
- 귓속말·노트·판정 팔레트 버튼이 추가되었습니다.
- 내용이 많아져도 창이 스스로 늘어나지 않고 내부에서 스크롤됩니다. 손잡이로 직접
  늘리는 것은 그대로 가능합니다.

### 디스코드 음성 연동
말하는 사람의 초상화가 채팅할 때처럼 전면으로 나옵니다. 참고해 주신 Discord Sync
모듈과 같은 구조로, **서버에서 도는 작은 봇**이 음성 채널에 들어가 "말하기 시작/끝"
신호만 받아 Foundry로 넘깁니다. 봇은 음성을 녹음하거나 해석하지 않습니다.

- Foundry 쪽: 설정 `디스코드 음성 브리지 주소`, `디스코드 ↔ Foundry 사용자 연결`
- 봇: `tools/discord-voice-bridge/` (설치 방법은 그 폴더의 README 참고)
- Foundry 내장 음성채팅을 쓰면 봇 없이 바로 동작합니다.

## 0.9.4 — 노트를 채팅 메시지 기반으로 재작성

### 노트가 플레이어에게서 동작하지 않던 문제
말씀하신 방향이 옳았습니다. 월드 설정에 저장하려다 보니 **키퍼 클라이언트만 쓸 수 있어서**
플레이어의 입력을 소켓으로 릴레이해야 했고, 그 릴레이가 실패하고 있었습니다. 복잡해진
원인은 우클릭 수정이 아니라 저장 위치였습니다.

이제 귓속말 패널과 같은 방식입니다. 노트는 모듈 플래그가 붙은 **ChatMessage**입니다.

- 릴레이가 필요 없습니다. 플레이어도 채팅 메시지는 직접 만들 수 있습니다.
- 권한 모델이 그대로 따라옵니다. 작성자는 자기 글을 수정·삭제할 수 있고 키퍼는 전부
  손댈 수 있습니다. (첨부해 주신 Chat Edit 모듈도 `message.isAuthor` + `message.update()`를
  쓰고 있어 같은 방식임을 확인했습니다.)
- 노트 메시지는 채팅 로그에서 숨겨져, 기록은 노트 창에서만 보입니다.
- 키퍼 접속 여부와 무관하게 동작합니다.

### 판정 팔레트 드래그 앤 드롭
드롭 시점에 시스템의 JSON 페이로드만 실려 오는 경우가 있었습니다. 그래서 JSON이 들어오면
링크 마크업을 **역조립**하도록 했습니다. 속성 이름은 실제로 시스템이 렌더한 링크에서
가져왔으므로 형태가 일치합니다. 이름도 페이로드의 `label`에서 자동으로 채워집니다.
드래그 캡처가 성공하면 원본 HTML을 그대로 쓰고, 실패하면 이 경로로 복구합니다.

### 스테이지 여백
좌측 224px, 우측 104px로 넓혔습니다. 씬 컨트롤이 두 열로 늘어난 상태에서도 초상화가
아이콘 위로 올라오지 않습니다.

### 음성 활성화 (준비 작업)
말하는 사람의 초상화가 채팅과 똑같이 전면으로 나오는 기능의 토대를 넣었습니다.

- Foundry 내장 A/V를 쓰는 테이블은 **바로 동작합니다** (`rtcUserSpeaking` 훅).
- 디스코드는 외부 다리가 필요합니다. 그 진입점으로
  `game.coc7koStage.setSpeaking(userId, speaking)` 를 공개했습니다.

## 0.9.3 — 초상화가 사라진 원인 수정 · 팔레트 드래그 · 스테이지 배치

### 초상화·이름이 안 나온 진짜 이유
제공해 주신 에러 로그에 답이 있었습니다.

```
Caused by: ReferenceError: isGmUser is not defined
  at decorateMessage (chat-enhancements.js:217)
```

0.9.0에서 채팅 탭 기능을 제거할 때 **`isGmUser` 헬퍼까지 함께 삭제**했습니다. 이 함수는
모든 메시지 처리 과정에서 호출되므로, `renderChatMessageHTML` 훅이 매번 예외로 중단되고
초상화·플레이어 이름·헤더 정리가 전부 적용되지 않았습니다. 함께 주신 DOM 출력으로
`.message-header`와 `.message-sender`가 v14에도 그대로 있다는 것이 확인되어, 0.9.2에서
세웠던 "코어 클래스 이름이 바뀌었다"는 가정도 틀렸음이 드러났습니다. 헬퍼를 복원했고,
다른 함수가 같이 사라지지 않았는지 정적 검사로 확인했습니다.

### 공유 노트: 플레이어 입력이 조용히 실패하던 문제
전송 직전에 실행되던 **작성 중 임시 저장 플래그 처리**가 원인으로 보입니다. 그 쓰기가
거부되면 예외가 이벤트 핸들러에 묻혀 사라지고, 전송은 아예 시도되지 않은 채 아무 일도
일어나지 않습니다.

- 전송을 **먼저** 하고 플래그 정리는 나중에 하도록 순서를 바꿨습니다.
- 모든 단계를 try/catch로 감싸고 실패 시 입력 내용을 되돌려 놓습니다.
- 전송과 수신 양쪽에 콘솔 로그를 남깁니다.
- 릴레이가 2.5초 안에 반영되지 않으면 플레이어에게 경고합니다. 이제 실패해도 조용히
  묻히지 않습니다.

### 판정 팔레트: 드래그 앤 드롭
채팅의 판정 링크를 **팔레트 창으로 끌어다 놓으면 바로 등록**됩니다. 드롭 이벤트에는
요소가 실려 오지 않으므로, 드래그가 시작되는 순간 그 링크 요소를 붙잡아 두었다가
드롭 시점에 사용합니다. 저장하는 것은 시스템이 만든 링크의 HTML 원본 그대로이므로
데이터 속성이 하나도 손실되지 않습니다. 클립보드 붙여넣기 방식도 그대로 씁니다.

### 스테이지 배치
- 안전 여백을 넓혔습니다. 좌측은 씬 컨트롤 아이콘을 확실히 비켜가고, 하단은 해상도가
  낮아도 이름표가 잘리지 않도록 여유를 크게 늘렸습니다.
- PC 5·6은 NPC 블록보다 더 위에 배치해 4명이 2줄로 설 때도 겹치지 않습니다.
- **NPC는 화면 하단 중앙에 블록으로 모입니다.** 인원수에 따라 자동으로 배치가 바뀝니다.
  1명은 중앙, 2명은 나란히, 3명은 삼각형, 4명은 2×2 정사각형이며 위쪽 줄이 3·4번입니다.
- **비어 있는 NPC 자리는 아예 그리지 않습니다.** 회색 플레이스홀더가 무대 한가운데
  남지 않고, 남은 인원만으로 항상 균형 잡힌 배치가 됩니다.

## 0.9.1 — 채팅 카드 수정 · 귓속말/노트 독립 창

### 채팅 카드가 깨지던 문제
증상 두 가지(시간이 이름 왼쪽으로 감, 초상화가 안 나오거나 거대하게 나옴)는 원인이
같았습니다.

채팅 사이드바를 숨기고 채팅하면 Foundry는 카드를 **떠 있는 미리보기 팝오버**에 그립니다.
이전 버전은 이 팝오버의 메시지를 의도적으로 건드리지 않았는데, 그러면 초상화와 메타
컨테이너가 만들어지지 않은 채 카드 레이아웃(그리드)만 적용됩니다. 그 결과 시간과 삭제
버튼이 초상화 자리로 자동 배치되어 이름 왼쪽에 찍혔습니다. 초상화가 거대해진 것도
같은 이유로, 크기 지정 규칙이 컨테이너에 묶여 있어 팝오버에서는 적용되지 않았습니다.

- 팝오버의 메시지도 동일하게 처리합니다.
- 메시지 스타일을 **컨테이너가 아니라 메시지 자신의 클래스**에 걸도록 바꿨습니다.
  사이드바든, 팝아웃 창이든, 미리보기 팝오버든 동일하게 렌더됩니다.
- 초상화는 어떤 상황에서도 42px 썸네일로 고정되도록 독립 규칙을 추가했습니다.

### 귓속말 / 노트 — 채팅 탭 대신 독립 창으로
채팅 탭은 포기했지만 기능 자체는 살렸습니다. 사이드바에 끼어드는 대신 퀵바와 같은
**떠 있는 창**으로 만들었습니다. `<body>`에 `fixed`로 붙으므로 사이드바가 다시 그려져도
영향을 받지 않습니다. 탭이 간헐적으로만 눌리던 문제가 구조적으로 생길 수 없습니다.

- **귓속말 창**: 키퍼가 관여한 귓속말만 모아서 보여줍니다. 플레이어에게는 하단에
  빠른 입력칸이 있어 Enter로 키퍼에게 바로 귓속말을 보낼 수 있습니다.
- **노트 창**: 자유 기록장입니다. 내 계정(User 플래그)에 자동 저장되고, 저널로 내보내거나
  클립보드로 복사할 수 있습니다.
- 둘 다 헤더를 잡아 이동하고 우하단 그립으로 크기를 조절하며, 위치·크기·열림 상태가
  클라이언트별로 기억됩니다.
- 여는 방법: 씬 컨트롤(토큰 탭)의 자물쇠 아이콘과 책 아이콘, 또는 콘솔에서
  `game.coc7koPanels.whisper()` / `game.coc7koPanels.notes()`

## 0.9.0 — 기능 정리

### 제거: 채팅 탭 (전체 / 귓속말 / 노트)
사이드바 DOM에 직접 끼어드는 방식이었는데, v14에서 채팅 영역이 재렌더될 때마다 위치와
구조가 달라져 클릭이 간헐적으로만 먹히는 문제를 끝내 잡지 못했습니다. 위험 대비 이득이
맞지 않아 기능 자체를 들어냅니다. 관련 설정, CSS, 노트 패널도 함께 제거했습니다.

남아 있는 채팅 기능은 사이드바 구조에 의존하지 않으므로 그대로 동작합니다.
연속 발언 병합, 초상화와 이름 표시, 글꼴, 판정 난이도 색 구분, 입력 표시 예약 공간.

### 제거: 조사자 생성 마법사의 `전체 재굴림` / `잠금 해제` 버튼
전체 재굴림은 시스템 설정 `능력치를 다시 굴림`으로 원래 지원되는 기능이었습니다.
모듈이 중복으로 제공할 이유가 없어 삭제했습니다.

### 추가: 조사자 생성 굴림 전체 공개
생성 마법사가 열려 있는 동안 발생하는 비밀 굴림(키퍼에게만 보이는 굴림)을 모두에게
보이도록 바꿉니다. 마법사가 닫혀 있을 때의 비밀 굴림은 건드리지 않으므로, 플레이 중
키퍼의 일반 비밀 굴림에는 영향이 없습니다. 공개 범위를 넓히기만 하고 좁히지는 않습니다.

설정: `조사자 생성 굴림 전체 공개` (월드 설정, 기본 켜짐)

### 유지
창 크기 조정, v14 다이얼로그 레이아웃 방어, 스테이지, 퀵바, 팔레트, 말풍선, 화자 고정은
그대로입니다.

# 0.8.10

- Fixed the actual Foundry v14 sidebar/chat displacement regression.
- Removed `.coc7ko-resizable { position: relative; }`, which was overriding ApplicationV2's absolute/fixed positioning.
- This module's resize grip no longer turns floating windows such as Module Management into `<body>` flex items.
- Added a defensive position check after attaching an external resize grip.
- Added `game.coc7koWindowTools.repairFloatingWindows()` for one-shot repair of already-open affected windows.
- Expanded `game.coc7koWindowTools.inspect()` with parent, computed position, geometry, and CoC7-KO resize-class diagnostics.

# 0.8.9

- Foundry VTT v14: fixed DialogV2 windows participating in the `<body>` flex layout and squeezing `#interface`.
- Open direct-child `<dialog>` elements are now always taken out of normal flow, while preserving Foundry-provided window coordinates.
- Fixed the previous guard which incorrectly assumed an inline `left`/`top` meant the dialog was already out-of-flow.
- Body observer now watches descendant dialog `open`, `style`, and `class` changes.
- Added `game.coc7koWindowTools.inspectDialogs()` diagnostic helper.

# CoC7-KO 변경 이력

## 0.8.8 — 열려 있는 다이얼로그도 레이아웃에서 분리

0.8.7은 **닫힌** 다이얼로그만 처리해서, "업데이트 필요" 같은 **열려 있는** 다이얼로그에는
효과가 없었습니다.

### 원인
같은 문제의 다른 절반입니다. v14는 `<body>`를 flex 컨테이너로 쓰는데, 위치가 지정되지 않은
채 렌더된 다이얼로그는 **열려 있는 동안 flex 항목으로 자기 열을 차지합니다.** 그만큼
`#interface`가 좁아지고, 다이얼로그가 떠 있는 내내 UI 전체가 왼쪽으로 몰립니다.

### 조치
- **CSS**: `body > dialog[open]:not([style*="left"])`를 `position: fixed`로 띄우고 화면
  중앙에 배치합니다. 인라인 `left`를 가진 다이얼로그 — 즉 Foundry가 직접 위치를 잡은
  드래그 가능한 창 — 는 선택자에서 제외되므로 영향을 받지 않습니다. 스크립트가 반응하기
  전에 적용되어 인터페이스가 잠깐이라도 좁아지지 않습니다.
- **스크립트**: `<body>`의 자식 추가와 `open` 속성 변화를 감시해, 같은 조건의 다이얼로그를
  인라인 스타일로 확실히 띄웁니다. 코어가 렌더 훅 없이 여는 다이얼로그까지 잡기 위한
  이중 안전장치이며, 이미 처리한 요소는 표시해 두어 반복되지 않습니다.
- 0.8.7의 닫힌 다이얼로그 정리도 그대로 함께 동작합니다.

설정의 `흐름에 낀 다이얼로그 띄우기`로 끌 수 있고, 수동 실행은 `game.coc7koWindowTools.sweep()`
입니다.

## 0.8.7 — 인터페이스가 좁아지던 문제의 실제 원인 수정

### 확인된 원인
현장 진단으로 원인이 확정되었습니다. 사이드바가 밀린 것이 아니라 **`#interface` 자체가
창보다 좁게 잡혀 있었습니다.**

- 창 폭 1826px, `#interface` 폭 **1281px**
- `<body>`의 직계 자식으로 **`<dialog>` 하나가 `position: relative`, 폭 545px로 남아 있음**
- 1281 + 545 = 1826 — 정확히 일치

v14는 `<body>`를 flex 컨테이너로 쓰고 다이얼로그를 그 아래에 붙입니다. 닫힌 `<dialog>`는
브라우저 기본 스타일상 `display: none`이어야 하는데, `open` 속성이 빠진 채 display 값만
살아 있으면 **보이지 않으면서도 flex 항목으로 가로 공간을 계속 차지합니다.** 그만큼
`#interface`가 줄어들고, 그 안에서 우측 끝에 붙는 사이드바가 화면 중앙으로 끌려옵니다.

메시지 삭제나 창 열기 같은 동작 뒤에 나타난 것은, 그때 다이얼로그가 생겼다 사라지면서
잔재가 남았기 때문입니다.

### 조치
두 겹으로 막았습니다.

1. **CSS**: `body > dialog:not([open])`에 `display: none`을 강제합니다. 브라우저 기본
   동작을 되돌리는 것뿐이며, `show()`/`showModal()`로 연 다이얼로그는 항상 `open` 속성을
   가지므로 영향을 받지 않습니다.
2. **스크립트**: 창이 닫힐 때마다 `<body>` 아래의 닫힌 `<dialog>` 잔재를 제거합니다.
   살아 있는 애플리케이션이 소유한 요소는 건드리지 않습니다.

설정의 `닫힌 다이얼로그 잔재 정리`로 끌 수 있고, 수동 실행은 아래와 같습니다.

```js
game.coc7koWindowTools.sweep()
```

### 참고
0.8.3부터 0.8.6까지 제가 채팅 코드와 창 크기 조정을 원인으로 지목했던 것은 오진이었습니다.
다만 그 과정에서 넣은 방어(상시 UI에 `setPosition()`을 호출하지 않도록 한 것, 채팅 루트
판정 강화, 알림 팝오버 제외)는 그 자체로 올바른 수정이라 그대로 유지합니다.

## 0.8.6 — 코어 UI 차단 강화 · 레이아웃 진단 도구

인터페이스가 어긋나는 문제가 남아 있어, 방어를 한 겹 더 두고 원인을 특정할 진단 도구를
넣었습니다.

- 창 크기 조정이 **코어 UI 애플리케이션을 이름으로 차단**합니다. Sidebar, ChatLog,
  SceneControls, Players, Hotbar, SceneNavigation, CameraViews 등과 그 하위 클래스는
  어떤 경우에도 인라인 크기나 `setPosition()`을 받지 않습니다. 0.8.4의 "떠 있는 창인지"
  판정에 더해, 클래스 이름 기준의 이중 차단입니다.

### 진단 명령
콘솔에서 아래를 실행하면 원인을 좁힐 수 있습니다.

```js
game.coc7koWindowTools.inspectLayout()
```

`#interface`, `#board`, `#sidebar`, `#ui-right` 등의 위치·크기·**인라인 style**을 출력합니다.
이 모듈은 인라인으로 width/height/left/top 외에는 쓰지 않으므로, `inlineStyle`이 `(none)`인데도
영역이 어긋나 있다면 원인은 이 모듈 밖에 있습니다.

```js
game.coc7koWindowTools.forget()   // 저장된 창 크기를 전부 삭제하고 손잡이 제거
game.coc7koChat.safeMode()        // 채팅 탭 · 예약 공간 · 창 크기 조정 전부 off
```

## 0.8.5 — 채팅 탭 고정

탭 바가 메시지와 함께 스크롤되어, 위로 올리지 않으면 보이지 않던 문제를 고쳤습니다.

- 코어 버전에 따라 메시지 목록을 가리키는 요소가 **스크롤 컨테이너 자신일 때도 있고 그 안에
  중첩된 목록일 때도 있습니다.** 안쪽 목록 옆에 탭 바를 넣으면 스크롤 영역 *안에* 들어가게
  되어 같이 밀려 올라갑니다. 이제 스크롤이 실제로 일어나는 가장 바깥 요소를 찾아 그 위에
  탭 바와 노트 패널을 배치합니다.
- 만약의 경우를 대비해 탭 바에 `position: sticky`를 걸어, 어떤 레이아웃에서 스크롤 영역
  안에 들어가더라도 상단에 고정되도록 했습니다.
- 고정된 상태에서 메시지가 뒤로 비쳐 보이지 않도록 배경을 불투명하게 바꾸고 그림자를
  넣었습니다.

## 0.8.4 — 인터페이스 밀림의 진짜 원인 수정

0.8.3의 추정이 틀렸습니다. 원인은 채팅 코드가 아니라 **0.8.0에서 넣은 창 크기 조정
기능**이었습니다.

### 원인
Foundry v13 이후에는 사이드바, 씬 컨트롤, 플레이어 목록, 핫바 같은 **상시 UI도
ApplicationV2로 만들어집니다.** 이것들은 떠 있는 창이 아니라 `#interface` 안에서 주변
레이아웃이 위치를 잡아주는 영역입니다.

크기 조정 기능은 렌더되는 모든 ApplicationV2에 손잡이를 붙이려 했고, 저장된 크기가 있으면
인라인 width/height를 쓰고 `setPosition()`까지 호출했습니다. 상시 UI에 `setPosition()`을
부르면 **레이아웃으로 배치되던 영역이 절대 위치 창으로 바뀌면서** 통째로 어긋납니다.
사이드바가 오른쪽 끝을 벗어나 왼쪽으로 밀린 것이 정확히 이 현상입니다. 메시지 삭제나 창
열기처럼 재렌더가 일어날 때만 나타난 것도 이 때문입니다.

`#sidebar` 같은 id 제외 목록을 뒀지만, id가 다른 코어 버전이나 다른 UI 영역에는 걸리지
않았습니다.

### 조치
- 크기 조정은 이제 **진짜 떠 있는 창에만** 적용됩니다. 판정 기준은 세 가지를 모두 만족할
  것: `<body>`의 직계 자식일 것, 창 헤더가 있을 것, 계산된 position이 absolute/fixed일 것.
  `#interface` 안의 상시 UI는 구조적으로 이 조건을 통과할 수 없습니다.
- 저장된 크기는 **실제 id가 있는 창에만** 적용합니다. 이전에는 id가 없으면 클래스 이름으로
  키를 만들어서, 클래스가 비슷한 서로 다른 창이 같은 크기를 공유하는 문제가 있었습니다.

### 함께 고친 것
- **채팅 탭이 사라진 문제**: 0.8.3에서 채팅 루트 조건을 `#sidebar` 하위로 좁혔는데, 이 id가
  없는 레이아웃에서는 루트를 하나도 찾지 못해 탭이 아예 나타나지 않았습니다. 조건을
  "상시 인터페이스 안이거나 팝아웃 창 안"으로 바꿨습니다.
- **캔버스에 떠다니던 초상화**: 채팅 탭이 보이지 않을 때 Foundry가 띄우는 알림 팝오버에도
  아바타를 붙이고 있었습니다. 이제 팝오버 안의 메시지는 건드리지 않습니다.
- 탭 바·노트 패널은 메시지 목록의 부모가 채팅 루트 안에 있을 때만 삽입합니다.
  `#interface`나 `<body>` 같은 공용 컨테이너에는 절대 넣지 않습니다.
- `lang/ko.json`을 패키지에 포함했습니다.

### 안전 모드
문제가 재발하면 콘솔에서 한 번에 끌 수 있습니다.

```js
game.coc7koChat.safeMode()   // 채팅 탭 · 예약 공간 · 창 크기 조정 모두 off
game.coc7koChat.reset()      // 모듈이 넣은 채팅 요소만 제거
game.coc7koChat.inspect()    // 인식된 채팅 루트 확인
```

## 0.8.3 — 핫픽스: 채팅창이 좌측으로 밀리는 문제

메시지를 삭제하거나 창을 띄운 뒤 채팅 UI가 사이드바 밖으로 흩어지는 문제를 잡았습니다.

### 원인과 조치
채팅 영역을 찾는 조건이 너무 느슨해서, Foundry가 잠시 만들었다 없애는 컨테이너까지
채팅 루트로 잡고 그 안에 탭 바·노트 패널·예약 공간을 넣는 경우가 있었습니다. 그 컨테이너가
사라지거나 이동하면 우리 요소만 화면에 남아 떠다니게 됩니다.

- 채팅 루트로 인정하는 조건을 **메시지 목록을 실제로 가지고 있으면서 `#sidebar`나 팝아웃
  창 안에 있는 요소**로 좁혔습니다. 알림 팝오버나 툴팁 계열은 제외합니다.
- 설치할 때마다 **고아 요소 정리**를 먼저 수행합니다. 유효한 루트 안에 있지 않은 탭 바,
  노트 패널, 예약 공간은 즉시 제거됩니다.
- 타이핑 표시를 예약 공간으로 옮길 때, 메시지 목록·입력 폼·컨트롤 바 같은 **구조적 요소는
  절대 옮기지 않도록** 금지 목록을 넣었습니다. 사이드바가 뜯겨 나갈 수 있는 경로를 막습니다.
- 혹시 한 프레임이라도 요소가 밖으로 새어 나가면 보이지 않도록 CSS 안전망을 추가했습니다.

### 콘솔 탈출구
문제가 재현되면 아래를 쓰실 수 있습니다.

```js
game.coc7koChat.reset()    // 모듈이 넣은 채팅 요소만 제거하고 기본 채팅으로 복귀
game.coc7koChat.disable()  // 채팅 탭 기능 자체를 끄기
game.coc7koChat.inspect()  // 현재 인식된 채팅 루트 정보 출력
```

### 그 외
- `renderChatMessage` 훅 지원 경고를 없앴습니다. 이제 v12 이하에서만 등록하므로 v14
  콘솔에 더 이상 deprecated 경고가 찍히지 않습니다.

## 0.8.2 — 탭 클릭 문제 수정 · 화자 고정 · 매니페스트 정리

### 채팅 탭이 간헐적으로만 눌리던 문제
원인은 두 가지가 겹친 것이었습니다.

1. 채팅 영역을 찾는 선택자 여러 개가 **바깥 컨테이너와 안쪽 컨테이너에 모두 걸려**,
   탭 바가 두 개 만들어지는 경우가 있었습니다. 위에 겹친 쪽이 클릭을 먼저 먹으면서
   아래쪽 탭은 반응하지 않았습니다. 이제 가장 안쪽 요소 하나만 채팅 루트로 잡고,
   중복 삽입된 탭 바와 노트 패널은 발견 즉시 제거합니다.
2. 클릭 이벤트를 탭 바 요소에 직접 걸어 두었는데, 사이드바가 자주 다시 그려지는 탓에
   누른 요소가 클릭이 완료되기 전에 교체되는 경우가 있었습니다. 이제 document에
   **캡처 단계 위임 리스너** 하나만 두므로, 탭 바가 다시 만들어져도 클릭이 항상 통합니다.

덤으로 DOM 감시자가 채팅 메시지 추가나 FPS 표시 같은 잦은 변경은 무시하도록 바꿔서,
불필요한 재설치가 사라졌습니다.

### Stage 캐릭터로 화자 고정
- 토큰을 선택하지 않고 채팅을 쳐도, PC Stage에 배정된 **자기 캐릭터가 화자**가 됩니다.
- 판정, 아이템 카드, 귓속말, 이미 화자가 정해진 메시지에는 손대지 않습니다.
  비어 있는 화자만 채워 넣는 방식이라 기존 동작을 깨지 않습니다.
- 한 사용자가 Stage에서 소유한 PC가 **정확히 하나일 때만** 적용됩니다. 키퍼는 모든 액터를
  소유하므로 자동으로 제외되어, 여러 NPC를 번갈아 연기하는 데 지장이 없습니다.
- 설정의 `Stage 캐릭터로 화자 고정`으로 끌 수 있습니다.
- 참고: 토큰 없이 친 메시지는 Foundry가 OOC로 만들기 때문에 이 기능이 IC로 바꿉니다.
  순수한 OOC 발언이 필요하면 귓속말을 쓰거나 이 설정을 꺼주세요.

### 매니페스트
- 사용 중인 `module.json`(컴펜디엄 7종과 `lang/ko.json` 항목 포함)을 기준으로 삼고,
  0.8.x에서 추가된 `scripts/window-tools.js`와 `styles/window-tools.css`만 더했습니다.

## 0.8.1 — 채팅 헤더 겹침 수정 · 글꼴 개편 · 말풍선 방향

### 헤더 겹침 수정
- 캐릭터 이름, 플레이어 이름, 판정 설명줄, 시간, 삭제 버튼이 서로 겹쳐 찍히던 문제를
  해결했습니다. CSS로 위치를 추측하는 대신, 스크립트가 헤더의 요소들을 하나의 메타
  컨테이너로 모아 정리한 뒤 배치하므로 시스템이 헤더에 무엇을 넣든 겹치지 않습니다.
- 헤더는 3행 구조가 되었습니다. (초상화 | 캐릭터 이름 · 플레이어 이름 | 시간·버튼) 아래에
  판정 설명줄이 전체 폭으로 들어갑니다.

### 판정 설명줄 강조
- `자료조사 판정 (20%) - 보통 난이도` 같은 줄을 본문보다 크게, 굵게, 색 막대와 함께
  표시합니다.
- 난이도별 색 구분:
  - 보통 — 초록
  - 어려움 — 호박색
  - 극단적 — 적색
  - 대성공 — 보라
  한국어와 영어 표기를 모두 인식합니다.

### 글꼴 개편
샘플에 쓰신 조합이 Pretendard + 조선신명조였는데, 조선신명조는 무료로 쓸 수는 있어도
재배포·핫링크가 허용된 라이선스가 아니라 모듈에 포함하지 않았습니다. 대신 **이름/판정줄**과
**본문** 글꼴을 따로 고를 수 있게 하고, 상업적 사용이 명확히 허용된 폰트만 CDN으로
불러옵니다.

- 고딕: Pretendard(OFL), IBM Plex Sans KR(OFL), 고운돋움(OFL)
- 명조(조선신명조 대체): **KoPubWorld 바탕**(출판 본문용), **리디바탕**(OFL, 소설 본문체),
  고운바탕, 나눔명조, 송명, Hahmlet
- `시스템 기본 글꼴`을 고르면 아무것도 내려받지 않습니다.

기본값은 이름 = Pretendard, 본문 = KoPubWorld 바탕입니다. 명조가 부담스러우면 본문도
Pretendard로 바꾸면 됩니다. 인터넷이 막힌 클라이언트에서는 자동으로 로컬 글꼴로 대체됩니다.

### 말풍선 방향
- 좌측에 있는 포트레잇은 오른쪽으로, 우측에 있는 포트레잇은 왼쪽으로, 하단 중앙의 NPC는
  위쪽으로 말풍선이 열립니다.
- 배치 편집으로 카드를 옮긴 경우에는 옮겨진 실제 위치(가로 38% 미만 / 62% 초과 / 그 사이)를
  기준으로 방향을 정하므로, 자유 배치를 써도 말풍선이 화면 밖으로 나가지 않습니다.

## 0.8.0 — 창 크기 조정 · 조사자 생성 마법사 전체 재굴림

### 크기 조정을 지원하지 않는 창에 손잡이 추가
- 조사자 생성 마법사처럼 고정 크기로 만들어진 시스템/모듈 창의 우하단에
  크기 조절 손잡이를 붙입니다.
- 창 내용 영역에 스크롤을 자동으로 부여하므로, 창을 줄여도 내용이 잘리지 않습니다.
- 조절한 크기는 창 종류별로 기억되어 다음에 열 때 그대로 적용됩니다.
  손잡이를 **더블클릭**하면 저장된 크기를 잊고 원래 크기로 돌아갑니다.
- 설정에서 `모든 창 / 조사자 생성 마법사만 / 사용 안 함` 중 고를 수 있습니다(기본: 모든 창).

### 전체 재굴림
- 마법사 창 상단에 **전체 재굴림**과 **잠금 해제** 버튼이 생깁니다.
- `전체 재굴림`은 확인 후 마법사의 통합 굴림 버튼을 다시 눌러 능력치를 전부 새로 굴립니다.
- `잠금 해제`는 굴림 버튼과 입력칸의 비활성 상태만 풀어, 수동 입력이나 부분 조정이
  필요할 때 쓸 수 있습니다.

### 구현 방식에 대한 참고
시스템 코드를 덮어쓰거나 내부 함수를 가로채지 않고, 렌더된 DOM에서 잠긴 컨트롤을
찾아 해제한 뒤 **시스템 자신의 굴림 로직**을 다시 호출하는 방식입니다. 굴림 공식을
모듈이 흉내내지 않으므로 시스템이 업데이트되어도 규칙이 어긋나지 않고, 최악의 경우
선택자가 맞지 않아 기능이 동작하지 않을 뿐 월드가 깨지지 않습니다.

선택자는 설정에서 조정할 수 있으며, 콘솔에서 `game.coc7koWindowTools.inspect()`를
실행하면 현재 열린 창들의 id·클래스·제목과 인식된 굴림 버튼 목록을 출력합니다.

## 0.7.1 — 채팅 비주얼 리디자인

- 첨부해 주신 예시 스크린샷에 맞춰 채팅 카드를 다시 디자인했습니다.
  - 발언자 초상화(토큰 → 액터 → 유저 아바타 순) 42px 카드가 이름 왼쪽에 붙습니다.
  - 캐릭터 이름은 굵게, 그 **바로 아래**에 플레이어 이름이 작은 회색 글씨로 들어갑니다.
    (이전 0.7.0에서는 카드 하단에 있었습니다.)
  - 시간과 조작 버튼은 오른쪽 열로 정렬됩니다.
  - 카드 상단에 플레이어 색상 기반의 얇은 그라디언트 악센트 라인이 들어갑니다.
  - 연속 발언은 그림자·모서리까지 이어 붙여 하나의 카드처럼 보입니다.
- **채팅 카드 톤** 설정을 추가했습니다. 기본값 `밝은 카드`는 예시 이미지와 같은 아이보리
  톤이고, `어두운 카드`를 고르면 Foundry 기본 테마에 어울리는 짙은 카드가 됩니다.
- 탭 바를 세그먼트 컨트롤 형태로 다듬었고, 노트 탭도 같은 카드 언어를 쓰도록 맞췄습니다.
- 스테이지 말풍선도 같은 밝은 카드 + 같은 글꼴로 통일했습니다.
- `채팅 초상화 표시` 설정으로 아바타를 끌 수 있습니다.

## 0.7.0 — Foundry VTT v14 대응 UI 개편

### 1. 팔레트 분리 · 장면 퀵바 · 창 크기 조절
- **아이템 팔레트**와 **NPC 팔레트**가 각각 독립된 창으로 분리되었습니다.
  Scene Controls의 토큰 탭에 전용 아이콘이 생겼으므로, 장비 지급이나 NPC 교체를 위해
  매번 「장면 조정」 창을 열 필요가 없습니다.
- NPC HUD 표시 설정(`키퍼만 / 끄기`)은 NPC 팔레트 상단으로 이동했습니다.
- **장면 퀵바**(`fa-bookmark` 아이콘)가 추가되었습니다. 화면에 떠 있는 작은 패널로,
  북마크한 Scene을 한 번에 활성화하고, 현재 Scene의 장소 프리셋을 즉시 적용합니다.
  헤더를 잡아 이동하고 우하단 그립으로 크기를 조절하며, 위치·크기는 클라이언트별로 저장됩니다.
  헤더의 톱니 아이콘으로 북마크할 Scene 목록을 편집합니다.
- 모듈의 모든 창(장면 조정 / PC 설정 / 아이템 팔레트 / NPC 팔레트)에
  우하단 크기 조절 핸들이 생겼습니다.

### 2. 채팅 로그 개선
- 같은 화자가 연속으로 말하면 하나의 블록으로 **병합**됩니다.
  (기본 5분 이내, 설정에서 조정 가능. 주사위 굴림 카드는 병합하지 않습니다.)
- 병합된 블록 아래에 **실제 플레이어 이름**이 작게 표시됩니다.
  캐릭터 이름과 플레이어 이름이 같은 경우에는 표시하지 않습니다.
- 한국어 가독성을 고려한 글꼴 스택(Pretendard → Noto Sans KR → 시스템 순)으로 교체했으며,
  글꼴과 글자 크기는 설정에서 바꿀 수 있습니다.

### 3. 캐릭터 포트레잇 자유 배치
- Scene Controls의 `fa-arrows-up-down-left-right` 아이콘으로 **배치 편집 모드**를 켭니다.
- 카드를 드래그해 원하는 위치로 옮기고, 우하단 모서리를 끌어 크기를 조절합니다.
- 상단 툴바의 **잠금** 버튼으로 배치를 고정하고, **초기화**로 기본 배치로 되돌립니다.
- 배치는 월드 설정에 저장되어 모든 참가자에게 동일하게 보입니다.
  (기존의 클라이언트별 배율 설정은 그대로 유지됩니다.)

### 4. 스테이지 말풍선
- 캐릭터가 채팅으로 말하면 해당 포트레잇 위에 말풍선이 뜹니다.
- 표시 시간은 설정에서 2~20초로 조절할 수 있으며, 말풍선이 떠 있는 동안
  해당 포트레잇은 어두워지지 않습니다.
- 주사위 굴림, 시스템 카드, 볼 수 없는 귓속말은 말풍선으로 표시하지 않습니다.

### 5. 채팅 탭 분리
- 채팅 사이드바 상단에 **전체 / 귓속말 / 노트** 탭이 추가됩니다.
- **귓속말** 탭은 키퍼가 관여한 귓속말만 모아 보여주며, 개수 배지가 함께 표시됩니다.
- **노트** 탭은 자유 기록장입니다. 입력 내용은 본인 계정(User 플래그)에 자동 저장되며,
  버튼 하나로 저널 항목으로 내보내거나 클립보드로 복사할 수 있습니다.

### 6. 입력 표시 예약 공간
- 채팅 입력창 바로 위에 고정 높이의 빈 줄을 확보합니다.
- Cautious Gamemaster's Pack 등이 띄우는 「…님이 입력 중입니다」 표시를
  이 공간 안으로 옮겨, 표시가 나타나거나 사라져도 채팅 카드가 밀려 올라가지 않습니다.
- 높이(기본 26px)와 대상 CSS 선택자는 설정에서 바꿀 수 있고, 높이를 0으로 하면 비활성화됩니다.

### 호환성
- 최소 지원 버전이 Foundry VTT **v13**으로 올라갔습니다(v14 검증).
  ApplicationV2, `renderChatMessageHTML` 등 v13 이후 API를 사용합니다.
